#include "register_bank.hpp"
#include <fstream>

void RegisterBank::loadLayoutFromFile(std::string filePath)
{
	// parents need to be declared before kids
	// name, size, parent and offset
	// if something doesn't add up throw error and terminate

	
}